public interface ControleEnergia {

    //atributos na interface devem ter algum valor, não podem ter atributos de instância
    // void significa que não retorna nada
    void ligar();
    void desligar();
}
